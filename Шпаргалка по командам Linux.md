##  Строение команды

команда (что выполняем) {флаги (как выполняем)} {аргументы (над чем выполняем)} {опции}


mkdir имя_создаваемой_папки
touch имя_созваваемого_файла
rename имя_файла новое_имя_файла
mv  имя_файла путь_к_директории
cp путь_к_копируемому путь_куда_копировать
ls 
cd
rm
cat

pacman
yay

ls          # просто список
ls -l       # подробно (права, размер, владелец)
ls -a       # показать скрытые файлы (начинаются с .)
ls -h       # размеры в KB/MB/GB
ls -t       # сортировка по времени
ls -laht    # комбинация всех флагов

htop
btop